import { part1, part2 } from "./day.ts";
import { assertEquals } from "std/assert/mod.ts";
import { loadInput } from "../input.ts";
const input = loadInput("day0-template/input.txt");

const example = ``;

Deno.test("part1(example)", () => {
  assertEquals(part1(example), 0);
});

Deno.test("part1(input)", () => {
  assertEquals(part1(input), 0);
});

Deno.test("part2(example)", () => {
  assertEquals(part2(example), 0);
});

Deno.test("part2(input)", () => {
  assertEquals(part2(input), 0);
});
